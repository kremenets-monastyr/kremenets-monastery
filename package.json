{
  "name": "kremenets-monastery",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@fontsource/ibm-plex-sans": "^5.2.8",
    "@fontsource/monomakh": "^5.2.7",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  }
}
